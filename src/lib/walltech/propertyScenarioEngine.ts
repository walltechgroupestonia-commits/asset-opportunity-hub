import type{InvestmentAssumptions,InvestmentScenario}from"./propertyIntelligenceTypes";
const pct=(a:number,b:number)=>b>0?Math.round((a/b)*1000)/10:null;
export function buildInvestmentScenarios(a:InvestmentAssumptions):InvestmentScenario[]{const costKeys:[keyof InvestmentAssumptions,string][]=[["targetPurchasePrice","Prezzo acquisto"],["renovationCost","Ristrutturazione"],["proceduralCosts","Costi procedurali"],["taxesAndTransferCosts","Imposte/trasferimento"],["condominiumCosts","Costi condominiali"],["financeCosts","Costi finanziari"],["contingency","Fondo imprevisti"]];const miss=costKeys.filter(([k])=>a[k]===null).map(([,l])=>l);const vals=costKeys.map(([k])=>a[k] as number|null);const total=vals.some(v=>v===null)?null:(vals as number[]).reduce((s,v)=>s+v,0);const fm=[...miss,...(a.expectedSalePrice===null?["Valore di uscita"]:[])];const margin=total!==null&&a.expectedSalePrice!==null?a.expectedSalePrice-total:null;const flip={name:"FLIP" as const,completeness:Math.round(((8-fm.length)/8)*100),totalCapitalRequired:total,grossMargin:margin,roiPercent:margin!==null&&total!==null?pct(margin,total):null,annualGrossYieldPercent:null,missingInputs:fm};const rm=[...miss,...(a.expectedMonthlyRent===null?["Canone mensile"]:[])];const rental={name:"RENTAL" as const,completeness:Math.round(((8-rm.length)/8)*100),totalCapitalRequired:total,grossMargin:null,roiPercent:null,annualGrossYieldPercent:a.expectedMonthlyRent!==null&&total!==null?pct(a.expectedMonthlyRent*12,total):null,missingInputs:rm};const hm=[...rm,...(a.monthsToExit===null?["Orizzonte temporale"]:[])];return[flip,rental,{name:"HOLD",completeness:Math.round(((9-hm.length)/9)*100),totalCapitalRequired:total,grossMargin:null,roiPercent:null,annualGrossYieldPercent:a.expectedMonthlyRent!==null&&total!==null?pct(a.expectedMonthlyRent*12,total):null,missingInputs:hm}];}
